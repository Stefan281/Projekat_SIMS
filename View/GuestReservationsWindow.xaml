<Window x:Class="BookingApp.View.GuestReservationsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="My reservations"
        Height="450"
        Width="800"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <!-- back -->
            <RowDefinition Height="40"/>
            <!-- filter + cancel -->
            <RowDefinition Height="30"/>
            <!-- error -->
            <RowDefinition Height="*"/>
            <!-- grid -->
        </Grid.RowDefinitions>

        <!-- Back -->
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Button Width="80"
                    Height="25"
                    Click="Back_Click">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="←"
                               FontSize="16"
                               VerticalAlignment="Center"
                               Margin="0,0,5,0"/>
                    <TextBlock Text="Back"
                               VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </StackPanel>

        <!-- Filter + Cancel -->
        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    VerticalAlignment="Center">
            <TextBlock Text="Filter:"
                       VerticalAlignment="Center"
                       Margin="0,0,10,0"/>
            <ComboBox Width="150"
                      ItemsSource="{Binding FilterOptions}"
                      SelectedItem="{Binding SelectedFilter}"
                      VerticalAlignment="Center"
                      Margin="0,0,20,0"/>

            <Button Content="Cancel reservation"
                    Width="140"
                    Height="25"
                    Command="{Binding CancelCommand}"/>
        </StackPanel>

        <!-- Error -->
        <TextBlock Grid.Row="2"
                   Text="{Binding ErrorMessage}"
                   Foreground="Red"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"/>

        <!-- Rezervacije -->
        <DataGrid Grid.Row="3"
                  ItemsSource="{Binding Reservations}"
                  SelectedItem="{Binding SelectedReservation}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  HeadersVisibility="Column">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Request Id" Binding="{Binding RequestId}" Width="80"/>
                <DataGridTextColumn Header="Hotel" Binding="{Binding HotelCode}" Width="100"/>
                <DataGridTextColumn Header="Apartment" Binding="{Binding ApartmentName}" Width="150"/>

                <DataGridTextColumn Header="Start date" Width="120">
                    <DataGridTextColumn.Binding>
                        <Binding Path="StartDate" StringFormat="{}{0:yyyy-MM-dd}" />
                    </DataGridTextColumn.Binding>
                </DataGridTextColumn>

                <DataGridTextColumn Header="End date" Width="120">
                    <DataGridTextColumn.Binding>
                        <Binding Path="EndDate" StringFormat="{}{0:yyyy-MM-dd}" />
                    </DataGridTextColumn.Binding>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                <DataGridTextColumn Header="Rejection reason" Binding="{Binding RejectionReason}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>