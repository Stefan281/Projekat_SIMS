<Window x:Class="BookingApp.View.SignInForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookingApp.View"
        mc:Ignorable="d"
        Title="Sign in" Height="300" Width="450"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="120"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Please sign in"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   FontSize="18"
                   Margin="0,10,0,0"/>

        <Grid Grid.Row="1" Grid.Column="0" Width="300" Height="80">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Label Grid.Row="0" Grid.Column="0" Height="30">Email:</Label>
            <Label Grid.Row="1" Grid.Column="0" Height="30">Password:</Label>

            <TextBox Grid.Row="0"
                     Grid.Column="1"
                     Height="25"
                     VerticalAlignment="Center"
                     Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"/>

            <PasswordBox x:Name="txtPassword"
                         Grid.Row="1"
                         Grid.Column="1"
                         Height="25"
                         VerticalAlignment="Center"
                         PasswordChanged="PasswordBox_PasswordChanged"/>
        </Grid>

        <!-- Error message -->
        <TextBlock Grid.Row="3"
                   Text="{Binding ErrorMessage}"
                   Foreground="Red"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"/>

        <!-- Dugme za login koristi Command iz ViewModel-a -->
        <Button Grid.Row="2"
                Content="Sign in"
                Width="80"
                Height="25"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Command="{Binding LoginCommand}"/>
    </Grid>
</Window>
