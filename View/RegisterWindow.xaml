<Window x:Class="BookingApp.View.RegisterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Guest registration"
        Height="400"
        Width="450"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Register as guest"
                   FontSize="18"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"/>

        <Grid Grid.Row="1" Margin="0,10,0,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="JMBG:" VerticalAlignment="Center"/>
            <TextBox Grid.Row="0" Grid.Column="1"
             Margin="0,3,0,3"
             Text="{Binding Jmbg, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Grid.Row="1" Grid.Column="0" Text="First name:" VerticalAlignment="Center"/>
            <TextBox Grid.Row="1" Grid.Column="1"
             Margin="0,3,0,3"
             Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Grid.Row="2" Grid.Column="0" Text="Last name:" VerticalAlignment="Center"/>
            <TextBox Grid.Row="2" Grid.Column="1"
             Margin="0,3,0,3"
             Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Grid.Row="3" Grid.Column="0" Text="Email:" VerticalAlignment="Center"/>
            <TextBox Grid.Row="3" Grid.Column="1"
             Margin="0,3,0,3"
             Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Grid.Row="4" Grid.Column="0" Text="Phone:" VerticalAlignment="Center"/>
            <TextBox Grid.Row="4" Grid.Column="1"
             Margin="0,3,0,3"
             Text="{Binding PhoneNumber, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Grid.Row="5" Grid.Column="0" Text="Password:" VerticalAlignment="Center"/>
            <PasswordBox x:Name="txtPassword"
                 Grid.Row="5"
                 Grid.Column="1"
                 Margin="0,3,0,3"
                 PasswordChanged="PasswordBox_PasswordChanged"/>

            <TextBlock Grid.Row="6" Grid.Column="0" Text="Confirm password:" VerticalAlignment="Center"/>
            <PasswordBox x:Name="txtConfirmPassword"
                 Grid.Row="6"
                 Grid.Column="1"
                 Margin="0,3,0,3"
                 PasswordChanged="ConfirmPasswordBox_PasswordChanged"/>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <TextBlock Text="{Binding ErrorMessage}"
                       Foreground="Red"
                       VerticalAlignment="Center"
                       Margin="0,0,20,0"/>

            <Button Content="Cancel"
                    Width="80"
                    Margin="0,0,10,0"
                    Click="Cancel_Click"/>

            <Button Content="Register"
                    Width="80"
                    Command="{Binding RegisterCommand}"/>
        </StackPanel>
    </Grid>
</Window>