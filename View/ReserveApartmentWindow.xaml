<Window x:Class="BookingApp.View.ReserveApartmentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Reserve apartment"
        Height="450"
        Width="800"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <!-- back -->
            <RowDefinition Height="50"/>
            <!-- date -->
            <RowDefinition Height="30"/>
            <!-- error -->
            <RowDefinition Height="*"/>
            <!-- grid -->
            <RowDefinition Height="50"/>
            <!-- button -->
        </Grid.RowDefinitions>

        <!-- Back -->
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Button Width="80"
                    Height="25"
                    Click="Back_Click">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="←"
                               FontSize="16"
                               VerticalAlignment="Center"
                               Margin="0,0,5,0"/>
                    <TextBlock Text="Back"
                               VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </StackPanel>

        <!-- Date -->
        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    VerticalAlignment="Center">
            <TextBlock Text="Select date:"
                       VerticalAlignment="Center"
                       Margin="0,0,10,0"/>
            <DatePicker SelectedDate="{Binding SelectedDate, Mode=TwoWay}"/>
        </StackPanel>

        <!-- Error -->
        <TextBlock Grid.Row="2"
                   Text="{Binding ErrorMessage}"
                   Foreground="Red"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"/>

        <!-- Apartments grid -->
        <DataGrid Grid.Row="3"
                  ItemsSource="{Binding Apartments}"
                  SelectedItem="{Binding SelectedApartment}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  HeadersVisibility="Column">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Hotel code" Binding="{Binding HotelCode}" Width="100"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="150"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                <DataGridTextColumn Header="Rooms" Binding="{Binding RoomCount}" Width="80"/>
                <DataGridTextColumn Header="Max guests" Binding="{Binding MaxGuests}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Reserve -->
        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
            <Button Content="Reserve"
                    Width="100"
                    Height="30"
                    Command="{Binding ReserveCommand}"/>
        </StackPanel>
    </Grid>
</Window>